# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

builds:
  - id: superbank
    builder: rust
    command: zigbuild
    binary: superbank
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
    flags:
      - --release
      - --locked
      - --package=superbank
      - --bin=superbank
    env:
      - SUPERBANK_GIT_SHA={{ .Commit }}

  - id: superbank-rpc
    builder: rust
    command: zigbuild
    binary: superbank-rpc
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
    flags:
      - --release
      - --locked
      - --all-features
      - --package=superbank-rpc
      - --bin=superbank-rpc
    env:
      - SUPERBANK_GIT_SHA={{ .Commit }}
      - RUSTFLAGS=-C force-frame-pointers=yes

  - id: superbank-solparq
    builder: rust
    command: zigbuild
    binary: superbank-solparq
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
    flags:
      - --release
      - --locked
      - --package=superbank-solparq
      - --bin=superbank-solparq
    env:
      - SUPERBANK_GIT_SHA={{ .Commit }}

  - id: superbank-solparq-read
    builder: rust
    command: zigbuild
    binary: superbank-solparq-read
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
    flags:
      - --release
      - --locked
      - --package=superbank-solparq
      - --bin=superbank-solparq-read
    env:
      - SUPERBANK_GIT_SHA={{ .Commit }}

  - id: superbank-verify
    builder: rust
    command: zigbuild
    binary: superbank-verify
    targets:
      - x86_64-unknown-linux-gnu
      - aarch64-unknown-linux-gnu
    flags:
      - --release
      - --locked
      - --package=superbank-verify
      - --bin=superbank-verify
    env:
      - SUPERBANK_GIT_SHA={{ .Commit }}

archives:
  - id: superbank
    ids: [superbank]
    name_template: superbank-{{ .Tag }}-{{ .Os }}-{{ .Arch }}
    formats: [tar.gz]
    files: []

  - id: superbank-rpc
    ids: [superbank-rpc]
    name_template: superbank-rpc-{{ .Tag }}-{{ .Os }}-{{ .Arch }}
    formats: [tar.gz]
    files: []

  - id: superbank-solparq
    ids: [superbank-solparq]
    name_template: superbank-solparq-{{ .Tag }}-{{ .Os }}-{{ .Arch }}
    formats: [tar.gz]
    files: []

  - id: superbank-solparq-read
    ids: [superbank-solparq-read]
    name_template: superbank-solparq-read-{{ .Tag }}-{{ .Os }}-{{ .Arch }}
    formats: [tar.gz]
    files: []

  - id: superbank-verify
    ids: [superbank-verify]
    name_template: superbank-verify-{{ .Tag }}-{{ .Os }}-{{ .Arch }}
    formats: [tar.gz]
    files: []

checksum:
  name_template: SHA256SUMS.txt

release:
  name_template: "Release {{ .Tag }}"
  header: |
    rust {{ .Env.RUST_VERSION }}

changelog:
  sort: asc
  use: github
